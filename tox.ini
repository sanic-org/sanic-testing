[tox]
envlist = py36, py37, py38, lint

[testenv]
deps =
    -r{toxinidir}/requirements.testing.txt
commands =
    pytest {posargs:tests tests sanic_test}

[testenv:lint]
deps =
    flake8
    black
    isort

commands =
    flake8 sanic_test
    black --check --verbose sanic_test
    isort --check-only --recursive sanic_test

[pytest]
filterwarnings =
    ignore:.*async with lock.* instead:DeprecationWarning
